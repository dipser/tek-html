/**
 * jquery.treeview.js v{{version}}
 * - jquery plugin to create treeview -
 * @version v{{version}}
 * @author {{author}}
 * @date {{date}}
 */
(function (dependencies, undefined) {
{{modules}}

})({
    $: this['jQuery']
}, undefined);